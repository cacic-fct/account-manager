{
  "name": "account-privacy",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/account-privacy/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/libs/account-privacy"],
      "options": {
        "cwd": "libs/account-privacy",
        "command": "bunx ngc -p tsconfig.lib.json && cp package.json README.md ../../dist/libs/account-privacy/"
      }
    },
    "test-cov": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/libs/account-privacy"],
      "options": {
        "cwd": "libs/account-privacy",
        "command": "bun run jest --config jest.config.ts --coverage"
      }
    }
  }
}
